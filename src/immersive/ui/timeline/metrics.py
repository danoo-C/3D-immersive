"""The timeline's sizes, in one place for the view, the headers and the clips
to agree on - a header level with its lane is a header whose height is the
lane's."""

from __future__ import annotations

from typing import Final

#: A lane's height, and so a header's: room for the header's two rows.
LANE_HEIGHT: Final = 56
